{
  "name": "trainer",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --project tsconfig.client.json",
    "watch": "tsc --project tsconfig.client.json --watch",
    "serve": "npx serve public"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
