{
  "name": "trainer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "rundev": "tsc -p tsconfig.client.json && ts-node-dev --project tsconfig.json --respawn --transpile-only src/server.ts",
    "dev": "ts-node-dev --project tsconfig.json --respawn --transpile-only src/server.ts",
    "build:client": "tsc -p tsconfig.client.json",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^24.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3",
    "undici-types": "^7.16.0"
  }
}
